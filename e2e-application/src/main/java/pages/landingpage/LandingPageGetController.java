package pages.landingpage;

import bdd_models.GetController;
import scenarios_utils.CustomAssert;

public class LandingPageGetController extends GetController<CustomAssert> {

    public LandingPageGetController(CustomAssert customAssert) {
        super(customAssert);
    }
}
